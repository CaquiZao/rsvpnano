name: Build firmware assets

on:
  workflow_call:
    inputs:
      firmware_tag:
        required: true
        type: string
  # Disparo manual, porque até aqui a única forma de compilar firmware no CI era
  # publicar uma release: preview.yml e release.yml chamam este workflow, e os dois
  # criam release. Quem só quer o .bin de uma mudança — ou confirmar que ela compila
  # para todas as placas — não deveria ter de publicar nada para consegui-lo.
  workflow_dispatch:
    inputs:
      firmware_tag:
        description: "Tag aplicada localmente para estampar o build, ex.: manual-build"
        required: true
        default: "manual-build"
        type: string

permissions:
  contents: read

jobs:
  matrix:
    runs-on: ubuntu-latest
    outputs:
      environments: ${{ steps.environments.outputs.value }}
    steps:
      - uses: actions/checkout@v7
      - id: environments
        run: echo "value=$(python3 tools/export_web_firmware.py --list-envs)" >> "$GITHUB_OUTPUT"

  build:
    needs: matrix
    strategy:
      fail-fast: false
      matrix:
        environment: ${{ fromJSON(needs.matrix.outputs.environments) }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Cache PlatformIO
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
            ~/.platformio/packages
            ~/.platformio/platforms
          key: ${{ runner.os }}-pio-${{ hashFiles('platformio.ini', 'boards/**', 'tools/pio_*.py') }}
          restore-keys: ${{ runner.os }}-pio-

      - name: Install PlatformIO
        run: pip install platformio

      - name: Set local firmware tag
        env:
          RSVP_FIRMWARE_TAG: ${{ inputs.firmware_tag }}
        run: git tag -f "$RSVP_FIRMWARE_TAG" HEAD

      - name: Export firmware environment
        env:
          RSVP_FIRMWARE_TAG: ${{ inputs.firmware_tag }}
        run: python3 tools/export_web_firmware.py --env "${{ matrix.environment }}"

      - uses: actions/upload-artifact@v7
        with:
          name: firmware-${{ matrix.environment }}
          path: build/firmware
          if-no-files-found: error

  assemble:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          pattern: firmware-*
          path: build/firmware
          merge-multiple: true

      - name: Assemble firmware release
        run: python3 tools/export_web_firmware.py --assemble

      - uses: actions/upload-artifact@v7
        with:
          name: firmware-assets
          path: build/firmware
          if-no-files-found: error
